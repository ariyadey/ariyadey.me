import { DirectionalDirective } from "./directional.directive";

describe("DirectionalDirective", () => {
  it("should create an instance", () => {
    const directive = new DirectionalDirective();
    expect(directive).toBeTruthy();
  });
});
